# TODO

## Durées tab

- [ ] Add "Now" button on time fields — insert current time with one click
- [ ] Add input validation feedback — warn when coords are 0,0 or VA is 0 instead of silent failure
- [ ] Result field styling — left border works for all components but color/font changes don't apply to `gr.Number`
- [ ] **Typing into a time field gets randomly overwritten mid-keystroke** (observed 2026-08-24, manual testing during the Gradio upgrade; root cause precisely identified and one instance fixed 2026-08-28 — see below). `DureesCore.compute` echoes back whichever field is the active "anchor" for the current target, unmodified, in every branch: `start` when target=Arrivée (its only editable field), `arrival` when target=Départ (its only editable field), and both `start`+`arrival` when target=VA (all three of duration/start/arrival are editable then). Because the round trip isn't instant, that echo can land *after* a second keystroke has already changed the field, clobbering it back to a stale value. Already hit this same class of bug in `ArmyInputHTML` (see [[project_army_input_html]]) and in Playwright tests (see `feedback_gradio_textbox_fill.md`'s `keyboard.type` warning).
  - **Fixed for VA specifically (2026-08-28, `10105ff`):** VA has exactly this same self-echo shape (`compute`'s non-VA branches return `va` unmodified) and was the first instance actually diagnosed. Fixed via a generic `_skip_output(compute, index, *args)` engine helper (`durees.py`) that replaces one output slot with `gr.skip()` — VA's index is derived automatically from `spec.value_fields.index(spec.va_field)`, applied uniformly to all three modes, no per-spec changes needed. Regression test: `test_va_edit_does_not_echo_back_and_overwrite_mid_keystroke` (`test_durees.py`) — reproduces the exact clobber (typing "50" landing as "5") on pre-fix code, passes after. Note for whoever extends this pattern: the *first* version of that test passed even pre-fix because it waited between keystrokes, which lets the (harmless, identical-value) echo settle before the next press and never exposes the race at all — the real reproduction needs the second keystroke to fire *before* the first round trip completes, then assert only after both have settled.
  - **NOT fixed: `start` (Arrivée target) and `arrival` (Départ target), for Legacy's plain-Textbox fields and Hybrid's old plain-Textbox fields.** Both currently set `skip_index=None` deliberately (`_legacy_spec`/`_hybrid_spec`'s `editable_fields`), on the reasoning "echoing a plain Textbox back into itself is harmless" — which the VA fix shows is wrong for the identical reason. This is harder than VA's fix: VA's skip index is target-independent (always the same slot), but here *which* field needs skipping depends on the current `target` value (`start`'s slot when target=Arrivée, `arrival`'s slot when target=Départ, both when target=VA) — `_skip_output`'s current signature takes a static `index`, not a target-dependent one. Needs either a variant of `_skip_output` that takes the live `target` input and a target→index mapping, or computing the right index inline in each spec's `editable_fields` construction (can't be done generically like VA's, since it's mode-agnostic logic that needs a live input value, not just static spec data). Experimental's TimeInput fields already have `skip_index` set unconditionally per-field (from the 2026-08-28 string/native refactor) so likely aren't affected by *this* bug — though see the separate, already-tracked "Experimental's VA-target compute semantics diverge" entry below for a different, pre-existing issue in that same neighborhood (over-suppression via `skip="duration"`, not under-suppression).
- [x] Replace `interactivity_updates` server round-trip with client-side JS — **done** (`0fb9a38`, 2026-05-23), TODO left stale since. Shipped as per-choice `js=True` events: `SegmentedControl.on_choice(label)` fires `trigger('select_<choice>')`, backed by a dynamic `EventListener` exposed through `__getattr__`; each tab defines one zero-arg `@staticmethod` per mode returning hardcoded `gr.update(...)` literals (Groovy transpiler requirement — no ternary on variables). `interactivity_updates` no longer exists anywhere in the codebase. Live today in both Durées (`durees.py`) and Armées (`armees.py`) — `self._target_sel.on_choice(...)`/`self._tdp_sel.on_choice(...)` with `js=True, show_progress="hidden"`.

## Armées tab (was Pontes)

### Cleanup todos
- [ ] Move `_find_tdp_alli` into `nawminator` lib (currently in `nmsite/tabs/armees.py`) — pure game math, no UI dependency

### Bugs
- [x] ~~Add/remove row buttons unreliable~~ — **likely fixed** (`9389cc6`, 2026-05-22, same rework that addressed the speed regression), filed only ~12h earlier. That commit's own message calls out "Removed `.then(_on_list_change)` from add/all buttons (regression from `4e82796` that caused double execution)" — exactly the shape of an "unreliable" bug. No longer reproduces in manual testing (2026-08-24). Not covered by an automated test (`test_add_row`/`test_delete_row` in `tests/nmsite/ui/test_armees.py` are still empty `pytest.mark.skip(reason="TODO")` stubs), so this is corroborating evidence, not a hard guarantee — reopen if it resurfaces.

### Future improvements
- [ ] Compact stats display per row (HP, ATK, count) — always visible alongside the paste box
- [ ] Split by DMG — répartir variant that equalises attack power across parts; needs a stats/bonuses input

## Parsing

- [ ] **`parse_joueurs_text` now splits on a literal tab, reliability on mobile unverified** — the joueurs page's copy-paste columns became user-toggleable (players can hide/show Distance, Terrain, État, etc. independently), which needed a proper rewrite (`_find_joueurs_text_columns` in `src/nawminator/parsing.py`): columns are located dynamically (header text when recognizable, else by value shape — coord's brackets, tdc's digits-and-spaces). All 5 fields (coord, tdc, colo_name, player_name, alliance) are required columns — alliance is the only one allowed to be blank *per row*, not the only one allowed to be absent as a column — and the whole parse is rejected if any is missing rather than guessing. This also fixed the old multi-word-name-plus-blank-field ambiguity bug (tried several generic-whitespace heuristics — an alliance max-length constraint, an all-blank-alliance sanity check — none closed the gap without opening a different one; a literal tab has no such ambiguity). The tradeoff: tab reliability across platforms (mobile in particular) isn't verified yet — desktop browsers reliably preserve tabs when copying a rendered `<table>`, but mobile copy-paste might not. Testing against a real mobile paste sample is in progress.

## Tests

- [ ] **Durées UI test coupled to real parser** — `test_player_dropdown_fills_coordinates` (`tests/nmsite/ui/test_durees.py`) loads `players_fixture.html` and runs it through the real `joueurs_source_code_pat` parser just to populate the player dropdown. Any parser/format change (e.g. the NAW joueurs-page format change) breaks this test even though it has nothing to do with parsing logic. Decouple by mocking/stubbing the player list instead of routing through the real HTML fixture.

- [ ] **TimeInput tests target a Settings-tab-only demo harness (`ti_demo_a`..`e`), not the real fields.** Flagged by the user 2026-08-26 while scoping the legacy/hybrid/experimental config split: components that exist purely to be tested against shouldn't live inside the actual app, and tests exercising the real Durées/Synchro tabs should assert against the real fields they use in production, not a parallel demo instance. `tests/nmsite/ui/test_time_input.py` currently only covers `ti_demo_a`..`e` (gated by `Config.dev`); the wrap-around-carry and toolbar-button-visibility tests added there (2026-08-26) are examples of coverage that should eventually move to exercise `durees_*_new`/`synchro_time_input_new` directly instead. **Must be resolved before closing `feature/time-input-component`.**

## App / architecture

- [x] Upgrade Gradio 5 → 6 — **done** but with known regressions (see below).

- [ ] **Armées tab: replace the `N_MAX=8` static-row architecture with a client-side-managed list.** Originally filed 2026-05-20 as "Speed regression in Armées and Durées tabs" right after the Gradio 6 upgrade; the actual slowness was fixed two days later (see below) and TODO.md was never updated to match — reworded 2026-08-24 to describe what's still actually true.
  - **What was fixed** (`9389cc6`, 2026-05-22, "Rework Armées tab event wiring — targeted gr.update() to silence cascade"): the 3-round-trip cascade (`on_shift` → `_list_state.change` → `_on_list_change` → `_compute_total`) is now 2 hops — `_on_list_change` computes the TDP/duration fields inline instead of a separate chained `.then()`. The blanket "push values to all `N_MAX` slots" payload is gone — `_updates_add`/`_updates_toggle_all`/`_updates_repartir` (`armees.py`) send a no-op `gr.update()` to every row that didn't change. `on_army_change` short-circuits via `Army.__eq__` so a no-op edit never reaches `_list_state` at all, killing the cascade at the source. Net effect: no longer feels slow in practice (confirmed 2026-08-24).
  - **What's still true**: the row *list* itself is still server-managed — `N_MAX=8` fixed `Row` containers exist up front, and add/delete/répartir/toggle-all are still server round-trips that toggle `visible=True/False` on pre-rendered containers rather than a client-side-owned list. (Not to be confused with `ArmyInputHTML`, which already is a custom component — but it owns a single row's 15-unit-type *value*, not the list of rows.)
  - **Gradio-side**: gradio-app/gradio#12831 (the general Gradio-6-is-slower-per-update report this was originally pinned on) was closed by a maintainer with "Going to close for lack of followup" — closed for reporter inactivity, not a technical rejection of the report. Not blocking us either way since our own fix above addressed the actual symptom.
  - **Plan, if picked back up**: replace the static rows with `@gr.render` or a custom component that manages its own state client-side, eliminating server round-trips for structural operations entirely. Same direction as the time-input component TODO below. Given the 9389cc6 fix already removed the pain, this is now an architecture-cleanup nice-to-have, not an urgent fix.
  - ~~Gradio 6.10.0 pinned due to separate tab-freeze bug in 6.11+ (gradio-app/gradio#13285, fix pending in PR #13240)~~ — **resolved 2026-08-24**: fix shipped in 6.16.0 (PR #13240), upgraded to 6.25.0 (`pyproject.toml`). Verified: full test suite green (`just test`), and manually hammered tab switching in/out of Armées (dataframe/accordion-heavy tab, the exact trigger for #13285) with Playwright/Firefox against the dev server — no freeze, no console errors, no visual breakage.

- [ ] CSS modularity — move tab-specific CSS out of `app.py` (see TODO comment in that file)

- [ ] Time input component — reusable picker supporting both `HH:MM:SS` and ajhms notations
  - Both formats are interchangeable in the game (not "clock time vs duration", just two notations for the same value)
  - Affects multiple tabs; needs to work well on mobile (scroll/swipe) and desktop (mouse scroll)
  - Gradio's native `gr.DateTime` is too heavy (forces a date), plain `gr.Textbox` has no scroll/touch support
  - **Chosen direction: enhanced `gr.HTML`** — newer Gradio (5/6) makes `gr.HTML` powerful enough:
    - `server_functions`: Python functions callable directly from JS inside the component
    - `js_on_load` + `trigger`: JS can fire Gradio events natively
    - `head`: load external JS/CSS libraries (e.g. a touch-friendly time picker lib)
    - No Svelte build pipeline needed — stays within the existing project
  - Full custom Gradio component (Svelte) is the cleanest architecture but high setup cost; Gradio team aware of this pain point (issue #12074)
  - One component needed that accepts/displays both formats, auto-detecting or toggling between them
  - **Currently being resumed on `feature/time-input-component`** (rebased onto `main` 2026-08-24): a working `gr.HTML`-based component (`time_input.py`/`script.js`/`style.css`) covering duration/clock_time/datetime modes, AJHMS + HH:MM:SS toggle, quick-fill pills, copy, reset, scroll/keyboard/touch-editable segments; demoed in a Réglages/Settings tab; has its own Playwright UI test suite (`tests/nmsite/ui/test_time_input.py`), updated 2026-08-25 for the click-to-edit redesign (segments hidden behind a display badge until clicked — tests now enter edit mode first via a `_enter_edit_mode()` helper where needed) and passing. Also fixed along the way: two tests asserting on a `<textarea>`'s value used `to_contain_text` (checks rendered text content) instead of `to_have_value` (checks the DOM `.value` property) — a textarea's dynamically-set value was never going to show up in the former, a pre-existing bug unrelated to the redesign that earlier failures had been masking. Not on `main` yet.
    - ~~Bug: `datetime` edit-mode segment order didn't match display-mode order~~ — **fixed 2026-08-25**: `getSegmentsForFormat`'s `DD/MM/YYYY HH:MM:SS` branch (`script.js`) now builds an explicit `['day','month','year']` order intersected with the enabled keys, instead of filtering out of `META` (which always follows `SEG_META.datetime`'s fixed year/month/day declaration order regardless of format).
    - ~~Bug: scrolling down past 0 didn't borrow from the next bigger unit~~ — **fixed 2026-08-25**: `clampField()` (`script.js`) no longer hardcodes `Math.max(0, val)` for duration mode; it now lets the value go negative so `normalize()`'s underflow/borrow branch (which was already correct, symmetric to its carry branch) actually gets a negative value to act on. Verified: scrolling seconds down from all-zero now correctly rolls to 23:59:59 with days (the highest enabled unit) clamped at 0, matching how carry already worked in the up direction.
    - ~~Layout bug: fixed 90px button-row reserve overflowed for instances with more buttons~~ — **fixed 2026-08-25**: confirmed via measurement it wasn't a "container too narrow" issue (still overlapped by 29px even at a generous 636px-wide widget) — the reserve was just a constant sized for a smaller button set, while `demo E`'s 3 quick-fill pills + toggle + clear + copy render to ~287px. Now `.ti-display`/`.ti-field`'s right padding reads a `--ti-btns-reserve` CSS custom property (`style.css`, 90px fallback) that `script.js` sets once, after initial render, to `.ti-btns`'s actual measured width + a small margin — adapts automatically per instance (measured: 67px for `demo A`'s minimal toolbar up to 304px for `demo E`'s full set) since the button set is fixed at construction time and never changes at runtime.
    - Wheel-scroll "missed ticks" investigation (2026-08-25): root cause is confirmed to be Firefox's APZ (Asynchronous Panning and Zooming) content round-trip, not event coalescing tied to display refresh rate. A non-passive `wheel`/`touchmove` listener (required here since we call `preventDefault()`) makes APZ wait for a response from the content process before it'll trust our `preventDefault()` decision — deadline is 400ms on desktop, 600ms on mobile ([Firefox source docs](https://firefox-source-docs.mozilla.org/gfx/AsyncPanZoom.html)), and further physical wheel notches queue/coalesce (summed, not dropped — verified empirically) behind that round-trip. This is an architectural ceiling for any Firefox wheel-hijacking widget, not something fixable by making our handler faster (already tried — patched `render()` to update segment text in place instead of rebuilding the DOM per tick; didn't move the needle, as expected once the real mechanism was understood). Current state, all marked `TEMP EXPERIMENT` in `script.js`:
      - A `deltaY`-magnitude-scaled step count recovers the coalesced total correctly (verified against synthetic multi-notch deltas), scaled by a **self-calibrating per-notch unit estimate** rather than a hardcoded constant (an earlier hardcoded 102 broke immediately on a second physical mouse — no cross-hardware standard for pixels-per-notch exists at the browser-JS level, see the comment block above `widget.addEventListener('wheel', ...)` for the full writeup and sourcing). Calibration tracks the running *minimum* observed `|deltaY|`, reset after 200ms idle — mirroring the long-established jQuery Mousewheel plugin's `lowestDelta` pattern (deliberately not GCD, which was considered and rejected: GCD is corrupted by a sample landing on *either* side of the true unit, min-tracking only by one landing *below* it).
      - The highlight-pulse animation experiment (masking jumps as continuous-feeling motion) was tried and reverted — no perceptible improvement in manual testing, and the actual goal turned out to be *consistency* (a round-trip scroll returns to the same value) rather than visual smoothness, which the calibration approach addresses directly.
      - Known open edge case, deliberately not chased (not representative of normal usage): rapidly alternating up/down scrolling was observed to drift the value in one direction over time rather than cancelling out exactly. Root cause not identified — the leading hypothesis (asymmetric `Math.round()` behavior at `.5` boundaries) was tested and ruled out, since the reporter's hardware only ever produces exact multiples of its true per-notch unit, leaving no rounding error once calibration converges. Revisit if it turns out to matter in practice.
      - Touch/mobile already declares `touch-action: none` (`style.css`) — the one declarative escape hatch Mozilla's docs recommend for this exact APZ tax — but wheel has no equivalent CSS opt-out, so coarser-than-ideal wheel granularity may just be a ceiling to design around rather than solve outright.
    - A full code-review + simplify pass (2026-08-25, both delegated to subagents, both effort "high") went through the whole branch; all findings from both were fixed except one deliberately skipped efficiency item: threading a precomputed `displayState` through `render()`/`commit()`/`formatState()`'s call chain to avoid recomputing it 2-3x per tick — the arithmetic itself is cheap and the signature changes would touch many call sites for little real gain, so judged not worth it. Everything else (paste-parsing now routed through a `server_functions` call to `nm.utils.parse_ajhms` instead of a duplicate JS parser; segment metadata now Python-owned via `data-seg-meta` instead of independently re-declared in JS; button styling matched to `ArmyInputHTML`/`LevelsInputComponent`'s shared look; assorted dead code/duplication/hot-path caching cleanups) is done and verified (full suite green, plus live Playwright checks for anything not already covered by the test suite).
    - **Mode-config split landed** (2026-08-26 design, implemented via `docs/superpowers/plans/2026-08-26-time-input-mode-config.md`): `Config.time_input_mode` (`legacy`/`hybrid`/`experimental`) now decides at server startup which field set Durées/Synchro build — no more always-dual-mounted-and-toggled. `S1`=hybrid, `S2`=legacy, `DEV`=experimental.
    - **Follow-ups flagged by the mode-config split's final whole-branch review (2026-08-26), not merge-blocking but real:**
      - Legacy mode (originally `durees.py`'s `DureesLegacy` class, now `_legacy_spec` — see the composition refactor below; still what `S2` serves) lost its live E2E coverage: Task 3 of the mode-config plan repointed `test_durees.py` to `mode="hybrid"` only, invalidating the plan's own claim that legacy stayed covered by pre-existing tests. Its logic is an unmodified port, but nothing exercises its compute/interactivity/defaults live anymore. Fix: add a `DEV_LEGACY` config preset + a `_MODE_SERVERS["legacy"]` fixture entry, parametrize `test_durees.py` over `["legacy", "hybrid"]` (its selectors are valid in both).
      - Experimental mode's (originally `DureesExperimental`, now `_experimental_spec`) VA-target compute semantics diverge from Legacy/Hybrid: `TimeInput`'s empty state round-trips as `dt.time(0, 0)`, never `None`, so `DureesCore.times_to_secs` never falls through to `parse_ajhms(duration)` the way the old string-based fields do — the "type a duration → get a VA" path is effectively dead in experimental mode, and the echo-guard (`skip="duration"`) can suppress a duration-field correction the user should see. Undercuts the "fair apples-to-apples comparison" goal the mode split was built for. Needs either a way to track which field last triggered the compute (letting a duration edit stay authoritative in VA mode) or an explicit empty-vs-zero sentinel for `TimeInput`'s time fields.
    - **UNRESOLVED perf complaint (2026-08-27):** user reports the Durées tab feels "slightly very slow" right after the mode-config split merged. User confirmed running `DEV`/experimental at the time. The 2026-08-28 session below fixed three real, independently-motivated issues on that exact mode (Hybrid's redundant round trip doesn't apply to `DEV`, but Experimental's string/native conversion overhead and the VA self-echo bug both do) and the user confirmed the tab feels "much much better" afterward — but **the actual before/after timing measurement was never run**, so it's not confirmed whether those fixes account for all of it or whether there's more. User's own words after the fixes: "i think we could do a lot better." Next step: profile now that the known code-level issues are cleared out (see the composition-refactor entry below for what's already been ruled out/fixed) — `NMSITE_CONFIG=S2` vs `DEV_HYBRID` vs `DEV` timing comparison is still the recommended first diagnostic, per-interaction browser trace (client JS vs. network/queue vs. server compute vs. layout/paint) as a second step if the round-trip comparison alone doesn't explain it.
    - **Composition refactor landed** (2026-08-28): `durees.py`'s three parallel mode classes (`DureesLegacy`/`DureesHybrid`/`DureesExperimental`) replaced by one `Durees` engine class + a `DureesModeSpec` per mode (`_legacy_spec`/`_hybrid_spec`/`_experimental_spec`), composition over inheritance. Fixes the mode-config split leaving the wiring *shape* triplicated even after the `/simplify` pass above deduped the leaf-level logic — the user flagged this as a miss (inheritance/composition was left as an open choice and neither was actually used). Pure internal restructure, no behavior change, verified by the full pre-existing test suite passing unchanged (298 non-UI + 22 Durées tests). See `docs/superpowers/specs/2026-08-28-durees-composition-refactor-design.md` and `docs/superpowers/plans/2026-08-28-durees-composition-refactor.md`.
    - **`DureesCore.compute` made native, string handling pushed to specs (2026-08-28, `3a2c2f2`):** was string-primary — every native caller (Experimental, Hybrid's `*_new` mirror) had to convert native→string→native just to call it, up to 6 conversion calls per edit in Experimental specifically (confirmed by mapping every mode×trigger path's conversion count before touching code, per the user's explicit request to reason on the code before profiling). `DureesCore.compute`/`times_to_secs`/`apply_time_defaults`/`shift_time` now operate on `dt.time`/`dt.timedelta` throughout; `_experimental_spec` calls them directly (zero conversions now); `_legacy_spec`/`_hybrid_spec` do the one legitimate string-boundary conversion each needs. Also generalized the `skip=` echo-guard out of every mode's compute function into the shared `_skip_output` engine helper (see the "Typing into a time field..." entry above) — this is what made the VA fix a one-line, mode-agnostic change instead of needing a wrapper in every spec.

- [ ] General user feedback / error notifications
  - Gradio has built-in toast support: `gr.Warning()`, `gr.Error()`, `gr.Info()` callable from any event handler, no extra UI needed
  - Currently all tabs fail silently (bad inputs just return unchanged values with no message)
  - Plan: replace silent `return` fallbacks with `gr.Warning(...)` calls, use `gr.Error` for unexpected failures
  - Convention to establish: Warning = soft/missing input, Error = hard/unexpected failure
  - Each tab owns its own messages (no central registry needed given tab-based structure)
  - Start with Durées tab as a template, then roll out to other tabs

## Documentation conventions

- [ ] **Define a convention for hybrid human/AI code documentation, then refactor to match it.** A human reading code has small context but infers a lot from little; an AI agent (Claude) has much more context available per-session but none carried over between sessions, so it needs the "why"/design-rationale spelled out explicitly to avoid re-introducing bugs it already fixed once (e.g. the synchro tab's BBCode-link-in-shared-DataFrame bug). What's "enough" documentation differs a lot between the two audiences, and this project has no established docstring/comment convention to build on (most functions have none, by design — code is meant to be readable at a glance). Pattern applied twice now in `src/nmsite/tabs/synchro.py` (2026-08-23): `format_copy_data_table` first, then `format_copy_data_discord`/`_format_discord_row`/`_discord_row_widths` — short, caller-focused docstrings (what a function does) plus inline comments at the exact line each design decision applies to (why), rather than one dense docstring or a separate linked doc file. Consistent enough now to call a working pattern, not yet a written-down, generalizable guideline. Next steps: (1) look at what other projects/teams do for hybrid human/AI documentation for inspiration, (2) write the actual guideline once informed by that, (3) refactor the rest of the codebase's docstrings/comments to match (most files haven't been touched yet — this has so far only been applied where Claude happened to be already working).

## Devcontainer

- [ ] Upgrade base image back to `ubuntu` (latest) once Playwright supports Ubuntu 26.04 — pinned to `ubuntu-24.04` as a workaround. Track [microsoft/playwright#40117](https://github.com/microsoft/playwright/issues/40117); they plan to start work once a GHA runner image is available. Check back ~mid-June 2026.
